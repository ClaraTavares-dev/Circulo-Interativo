# 🔴 Círculo Interativo.
Este projeto demonstra o uso de **eventos em JavaScript** para criar uma **interação visual dinâmica**.  
A ideia é simples: um círculo na tela se move conforme o usuário movimenta o mouse, acompanhando suas coordenadas em tempo real.

## 🧠 Conceitos aplicados
- Detecção do movimento do mouse (`mousemove`)
- Manipulação de elementos do DOM
- Uso de `addEventListener`, `style.top` e `style.left`
